# robots.txt for tatendaz.github.io
# Served from the host root, so it governs every project page on this domain.

User-agent: *
Allow: /

# Project pages publish their whole docs/ folder, which includes the repo's
# internal dev docs. Those files are served raw (e.g.
# /yapui/summaries/2026-07-02-yapui-skill.md returns 200) and are thin,
# duplicate content that shouldn't compete with the landing pages in search.
# Everything here is already public on github.com — this is search hygiene,
# not access control.
Disallow: /*/features/
Disallow: /*/summaries/
Disallow: /*/sessions/

Sitemap: https://tatendaz.github.io/sitemap.xml
