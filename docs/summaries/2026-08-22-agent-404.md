# Session: Markdown guidance block on the 404 page (Is Agentic 81/100)

**Branch:** feat/agent-404
**Date:** 2026-08-22

## Prompts
1. "From my previous session please continue" (handoff: PR #9 open, five project PRs open,
   CodeRabbit queue, scanner puzzle).
2. "can i merge https://github.com/Tatendaz/Tatendaz.github.io/pull/9?"
3. "merged, run test-live and rescan"
4. "do it" — fix the last fixable scanner gap, "Agent-friendly 404s".
5. "and make sure the subpages achieve pairity also and use subagents to not fill the context
   window here"
6. "you can run npx is-agentic tatendaz.github.io to get a direct response agent friendly"

## Steps taken
- Drained the CodeRabbit queue one review per freed slot; fixed one minor finding per PR
  (docs nits, regex boundaries, H2 whole-line match), resolved the threads.
- PR #9 merged by the user; `make test-live` 6/6; fresh scan 72 → 81 ("Content without
  JavaScript" now passes: 2,187 chars, H1 + 3 H2s + 12 H3s).
- `npx is-agentic tatendaz.github.io --json` gave the untruncated evidence for the 404 check:
  "For full credit, include a short markdown body (site map links, where to look next)".
- Added the Markdown block to `404.html`, the `pre.md` style, static + live tests.
- Subagents added the same `docs/404.html` to the five project sites.

## Decisions
- Markdown inside a `<pre>` in the HTML 404 (GitHub Pages cannot serve `text/markdown` for
  404s); absolute URLs because the 404 is served at any path depth.
- No extra CodeRabbit re-review when CodeRabbit confirms a fix in-thread — saves the 5/hour
  budget.
